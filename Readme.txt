The code actually compares a bunch of enumerators and makes a new bunch of them,
placing the same elements in different arrays. If it tries to find an element in
1st enumerator and places it in 1st array, if it doesn't - it places nil there. 
